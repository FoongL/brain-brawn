import { Typography } from "@mui/material";
import React from "react";

const NRHeadings = () => {
  return (
    <Typography variant="h4" fontWeight={"700"} textAlign={"center"}>
      Click on each card exactly once!
    </Typography>
  );
};

export default NRHeadings;
